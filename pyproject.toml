[project]
name = "event-scanner"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.140", "uvicorn[standard]>=0.51", "httpx>=0.28", "feedparser>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
